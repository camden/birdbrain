{
  "name": "birdbrain-server",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": "12.x"
  },
  "license": "MIT",
  "scripts": {
    "build-client": "yarn --cwd client && yarn --cwd client build",
    "build-server": "tsc -p tsconfig.json",
    "build": "yarn build-client && yarn build-server",
    "dev-server": "ts-node-dev --respawn --transpileOnly ./server/server.ts",
    "dev-client": "yarn --cwd client start",
    "dev": "concurrently \"yarn dev-server\" \"sleep 2 && PORT=3001 yarn dev-client\"",
    "start": "node ./build/server.js",
    "postinstall": "yarn build-server"
  },
  "dependencies": {
    "@sentry/node": "5.11.1",
    "@sentry/types": "^5.11.0",
    "@types/express": "^4.17.2",
    "@types/socket.io": "^2.1.4",
    "express": "^4.17.1",
    "immer": "^5.3.2",
    "ramda": "^0.26.1",
    "redux": "^4.0.5",
    "reselect": "^4.0.0",
    "socket.io": "^2.3.0",
    "typescript": "^3.7.5",
    "uuid": "^3.4.0"
  },
  "devDependencies": {
    "@types/ramda": "^0.26.40",
    "@types/uuid": "^3.4.6",
    "concurrently": "^5.0.2",
    "ts-node-dev": "^1.0.0-pre.44"
  }
}
